# Tokens/sec Reporting

**Status:** completed

## Shipped

- Parse `eval_rate` and `prompt_eval_rate` from Ollama `/api/generate` JSON (with duration fallback).
- Display rates in post-create speed summary and auto-tune benchmark lines.
- Before/after comparison table when auto-tune runs.
- Extended `finetuna-results.json` with `before`, `after`, and rate fields.
